SendAfrica Python SDK — Starter Template
==========================================

This is a minimal, production-safe starter project for sending SMS with the
SendAfrica Python SDK.

## Setup

.. code-block:: bash

   pip install sendafrica
   cp .env.example .env
   # Edit .env and add your SA-... API key

## Run

.. code-block:: bash

   python main.py
